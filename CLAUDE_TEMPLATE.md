# CLAUDE.md — <repo-name>

> Per-repo memory. Keep it lean. Global rules, infra, gotchas live in
> `asawall/.github` (loaded via that repo's CLAUDE.md). This file is ONLY the
> facts specific to THIS repo. Outdated? Delete, don't append.

## What this repo is
- One-line purpose:
- Stack:
- Tenant (Vault workspace slug): tecmatiq | frageinen | planningx
- Prod location (server + path + ports):

## How it runs / deploys
- Local dev:
- Build/deploy path:
- Which reusable workflow(s) from asawall/.github it uses:

## Secrets it needs (Vault keys, not values)
-

## Repo-specific gotchas
- (traps unique to this repo; general ones go to asawall/.github/gotchas.md)

## Open threads
- (what's half-done, what's next)
